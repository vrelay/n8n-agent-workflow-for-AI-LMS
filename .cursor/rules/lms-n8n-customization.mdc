---
description: LMS n8n customization — comment-out style, UI find paths, rebuild workflow
alwaysApply: true
---

# AI LMS / n8n agent rules

Before editing n8n UI or Docker in this repo, read **`AGENTS.md`** at the repo root.

## Must follow

- All n8n code changes go in the **`n8n/` submodule** (fork `vrelay/n8n`, branch `ai-lms`), not by vendoring a copy elsewhere.
- Prefer **comment-out** with `LMS:` / `<!-- LMS: … -->` markers over deleting stock UI.
- Find UI via route → view → layout/slots; start searches under `n8n/packages/frontend/editor-ui/`.
- Node allowlist lives in `LMS_ALLOWED_NODE_TYPES` (`app/constants/nodeCreator.ts`); keep canvas `+` / search filtered to that list.
- After UI edits: `docker compose -f docker-compose.dev.yml exec n8n pnpm --filter n8n-editor-ui build` then `docker compose -f docker-compose.dev.yml restart n8n` (dev serves built assets, not live Vite by default).
- Commit submodule first, push `ai-lms`, then bump parent submodule SHA.
- Keep `AGENTS.md` “Already customized” inventory in sync when you add or restore LMS patches.

Do not invent a parallel frontend. Hide chrome; keep lesson workflows in parent `workflows/`.
